# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environment
venv/
.venv/
env/

# Data files (too large for git)
data/raw/
data/zenodo_test/
*.zip
*.csv.gz

# ML outputs
# NOTE: .joblib models are NOT ignored — they must be committed so the
# Render Docker build can bundle them into the backend image. They are
# only a few MB each (Isolation Forest on 96-feature data).
ml-pipeline/outputs/*.png
ml-pipeline/outputs/*.txt

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
